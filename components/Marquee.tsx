import type { ReactNode } from 'react';

interface MarqueeProps {
  children?: ReactNode;
  reverse?: boolean;
  className?: string;
}

export default function Marquee(_props: MarqueeProps) {
  return null;
}
